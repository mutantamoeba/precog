{
  "mcpServers": {
    "postgres-dev": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres@0.6.2"],
      "env": {
        "PGHOST": "localhost",
        "PGPORT": "5432",
        "PGDATABASE": "precog_dev",
        "PGUSER": "${DEV_DB_USER}",
        "PGPASSWORD": "${DEV_DB_PASSWORD}"
      }
    }
  }
}
