# Precog Test Environment Configuration
# ======================================
# Automated testing (CI/CD, pre-push hooks)
# Risk Level: LOW
#
# Usage (local testing):
#   cp config/.env.test.template .env
#   PRECOG_ENV=test python -m pytest tests/
#
# Usage (CI/CD):
#   Set environment variables in GitHub Actions workflow

PRECOG_ENV=test

# Database (isolated test database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=precog_test
DB_USER=precog_test_user
DB_PASSWORD=<test_password>

# Connection Pool (higher for parallel tests)
DB_POOL_MIN_CONN=2
DB_POOL_MAX_CONN=25

# API (use demo environment for tests)
KALSHI_API_KEY=<test_api_key>
KALSHI_API_SECRET=<path_to_test_private_key>
KALSHI_BASE_URL=https://demo-api.kalshi.com

# Logging (minimal for test speed)
LOG_LEVEL=WARNING
