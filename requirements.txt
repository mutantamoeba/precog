# Core Dependencies (Database & Config; from Checklist)
python-dotenv==1.1.1  # .env loading (Jun 24, 2025)
pyyaml==6.0.3  # YAML config parsing (Sep 25, 2025)
psycopg2-binary==2.9.11  # PostgreSQL adapter (binary for Windows; Oct 10, 2025)
sqlalchemy==2.0.44  # ORM (v2.0+; Oct 10, 2025)
alembic==1.15.2  # Schema migrations (Oct 5, 2025)

# API Clients (Sync/Async/WebSocket; from Checklist + Docs)
requests==2.32.5  # Sync HTTP fallback (Aug 18, 2025)
httpx==0.27.2  # Advanced HTTP (async/sync; Oct 8, 2025)
websockets==15.0.1  # WebSocket support (Kalshi; Sep 15, 2025)
aiohttp==3.13.0  # Async HTTP for API clients (WebSocket/REST hybrid; Oct 6, 2025)

# Data Processing (from Checklist + Docs)
pandas==2.3.3  # Data processing (odds matrices; Sep 29, 2025)
numpy==2.3.3  # Numerical computations (Sep 9, 2025)

# Utilities (Datetime + Validation/CLI/Auth/Scheduling/Parsing; Merged)
python-dateutil==2.9.1  # Date parsing (Sep 10, 2025)
pytz==2025.1  # Timezone support (Jan 2025 stable)
pydantic==2.12.0  # Data validation (config, prices; Oct 7, 2025)
click==8.1.7  # CLI (main.py entrypoint; stable)
cryptography==46.0.2  # RSA-PSS auth (Kalshi; Sep 30, 2025)
apscheduler==3.11.0  # Scheduling (market updates; Oct 2025)
spacy==3.8.7  # Text parsing (Master Req; May 23, 2025; post-install: python -m spacy download en_core_web_sm)

# Development Tools (Testing/Linting/Static Analysis; Trimmed for No Redundancy)
pytest==8.4.2  # Unit/integration tests (>80% coverage; Sep 4, 2025)
pytest-asyncio==0.24.1  # Async testing (Oct 1, 2025)
pytest-cov==5.1.1  # Coverage reporting (Sep 20, 2025)
black==24.8.0  # Code formatting (stable)
flake8==8.0.1  # Linting (PEP8/errors; Oct 2, 2025)
mypy==1.12.0  # Type checking (Oct 9, 2025)

# Monitoring and Logging (from Checklist)
structlog==25.4.0  # Structured logging (Oct 2025)