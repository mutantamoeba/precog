# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

# ============================================================================
# SECURITY - CREDENTIALS & KEYS (⚠️ NEVER COMMIT)
# ============================================================================

# Environment variables (contains passwords, API keys)
.env
.env.local
.env.*.local
.env.development.local
.env.staging.local
.env.production.local

# API Keys and Cryptographic Keys
_keys/
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*.der
*.pub
id_rsa
id_dsa
id_ecdsa
id_ed25519

# Database backups and dumps (may contain sensitive data)
*.dump
*.sql.bak
*.sql.backup
*.db.backup
*.sqlite.backup

# Configuration files with credentials (if any)
# config/*.local.yaml
# credentials.json
# secrets.yaml

# ============================================================================
# DEVELOPMENT ENVIRONMENT
# ============================================================================

# IDEs
.vscode/
.idea/
*.swp
*.swo
*.swn
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Development artifacts (project-specific)
_knowledge/
_sessions/
_archive/
archive/
backups/
.claude/
phase_reports/

# Session archives (local development context - already committed archives stay in git history)
docs/sessions/

# Development artifacts - phase completion reports, planning docs, analysis
# (historical versions remain in git history, current files stay local-only)
docs/phase-0-completion/
docs/phase-0.6-completion/
docs/phase-completion/
docs/phases-planning/
docs/phase05 updates/
docs/analysis/
docs/_archive/
docs/CLAUDE_CODE_IMPLEMENTATION_PLAN.md

# Session handoff (keep local-only, last committed version remains in git history)
SESSION_HANDOFF.md

# Testing (Phase 0.6c)
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.cover
.hypothesis/
.tox/
.nox/

# Test results (keep history.json, ignore timestamped runs)
test_results/*/
!test_results/README.md

# CI test result artifacts (downloaded from GitHub Actions)
test-results-*/
test-type-coverage-report/
test_warnings.txt

# Root-level development artifacts
phase05 updates/
PHASE_CHECKLISTS_AND_TEMPLATES.md

# Ruff cache
.ruff_cache/

# Mypy cache
.mypy_cache/

# Jupyter Notebook
.ipynb_checkpoints

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/
.pre-push-logs/

# Temporary files
*.tmp
*.temp
temp/
tmp/

# Documentation builds
docs/_build/
site/
_wip/

# ============================================================================
# DATA FILES (Downloaded by user, not committed)
# ============================================================================
# Historical Elo data (large CSV files downloaded from external sources)
data/historical/*.csv
# Keep README files to document data sources
!data/historical/README.md
docs/planning/
